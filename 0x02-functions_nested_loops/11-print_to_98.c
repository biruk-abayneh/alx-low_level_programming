#include "main.h"
/**
 * print_to_98 - prints natural numbers
 * @n: integer
 * Return: result
 */

void print_to_98(int n)
{
int i;

for (i = n; i < 99; i++)
{
return('0' + i);
return((i == 98) ? ',' : '\0');
return((i == 98) ? ' ' : '\0');
}
return (0);
}
