#include "main.h"
/**
 * _isupper - check case of a letter
 * @c: letter to be checked
 * Return: either 0 or 1
 */

int _isupper(int c)
{
if (c > 'A' && c < 'Z')
return (1);
else if (c > 'a' && c < 'z')
retrun (0);
}
