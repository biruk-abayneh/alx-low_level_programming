#include <stdio.h>
#include "main.h"
/**
 * print_numbers - print digit
 * Return: digits 
 */
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    print_numbers();
    return (0);
}
void print_numbers(void)
{
  int i;

  for (i = 0; i < 10; i++)
    {
      putchar('0' + i);
    }
  putchar('\n');
}
