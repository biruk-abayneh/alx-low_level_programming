#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

char *string_toupper(char *n)
{
int i;

i = 0;
while (n[i])
{
if ('a' <= n[i] && n[i] <= 'z')
{
n[i] = n[i] - 32;
}
i++;
}
return (n);
}
