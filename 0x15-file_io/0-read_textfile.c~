#include "main.h"

ssize_t read_textfile(const char *filename, size_t letters)
{
    FILE *a;
    int i = 0;
    
    a = fopen(filename, "r");
    for (i = 0; i < letters; i++)
    {
	_putchar(a);
	a++;
    }
    return ()
    
}